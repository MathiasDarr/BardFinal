import { createReducer  } from "redux-act";

import {
    queryCollections
} from 'actions';


const initialState = {};

export default createReducer(
    {
        [queryCollections.]
    }, initialState);